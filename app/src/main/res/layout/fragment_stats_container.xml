<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    >

    <!--app:stl_defaultTabBackground="?attr/selectableItemBackground"-->

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="@dimen/tab_bar_height"
        android:scrollbars="none"
        >

        <com.ogaclejapan.smarttablayout.SmartTabLayout
            android:id="@+id/tabLayout"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingLeft="@dimen/activity_horizontal_margin_small"
            android:paddingRight="@dimen/activity_horizontal_margin_small"
            android:paddingTop="@dimen/activity_vertical_margin_small"
            app:stl_defaultTabBackground="@android:color/transparent"
            app:stl_defaultTabTextColor="@color/color_state_stats_tab"
            app:stl_defaultTabTextMinWidth="@dimen/tab_min_width"
            app:stl_defaultTabTextSize="@dimen/text_size_stats_tabs"
            app:stl_distributeEvenly="false"
            app:stl_dividerColor="@android:color/transparent"
            app:stl_dividerThickness="0dp"
            app:stl_indicatorColor="@color/colorAccent"
            app:stl_indicatorCornerRadius="18dp"
            app:stl_indicatorGravity="center"
            app:stl_indicatorInterpolation="linear"
            app:stl_indicatorThickness="@dimen/tab_bar_indicator_height"
            app:stl_indicatorWidth="@dimen/tab_bar_indicator_width"
            app:stl_underlineColor="@android:color/transparent"
            app:stl_underlineThickness="0dp"
            />

    </HorizontalScrollView>


    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginLeft="@dimen/activity_horizontal_margin_small"
        android:layout_marginRight="@dimen/activity_horizontal_margin_small"
        android:background="@drawable/divider_horizontal_black"
        />

    <android.support.v4.view.ViewPager
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        />

</LinearLayout>