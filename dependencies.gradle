apply from: "versions.gradle"

allprojects {
    repositories {
        jcenter()
        google()
        maven { url "https://maven.google.com" }
    }
}

ext {

    support = [
            "com.android.support:support-v4:${versions.supportLib}",
            "com.android.support:appcompat-v7:${versions.supportLib}",
            "com.android.support:design:${versions.supportLib}",
            "com.android.support:cardview-v7:${versions.supportLib}",
            "com.android.support:customtabs:${versions.supportLib}"
    ]

    rx = [
            "io.reactivex.rxjava2:rxkotlin:${versions.rxKotlin}",
            "io.reactivex.rxjava2:rxandroid:${versions.rxAndroid}",
            "com.jakewharton.rxbinding2:rxbinding:${versions.rxBinding}"
    ]

    retrofit = [
            "com.squareup.retrofit2:retrofit:${versions.retrofit}",
            "com.squareup.retrofit2:converter-gson:${versions.retrofit}",
            "com.squareup.retrofit2:adapter-rxjava2:${versions.retrofit}"
    ]

    smartTabLayout = [
            "com.ogaclejapan.smarttablayout:library:${versions.smartTabLayout}@aar",
            "com.ogaclejapan.smarttablayout:utils-v4:${versions.smartTabLayout}@aar"
    ]

    kotlinLib = "org.jetbrains.kotlin:kotlin-stdlib:${versions.kotlin}"
    dagger = "com.google.dagger:dagger:${versions.dagger}"
    daggerCompiler = "com.google.dagger:dagger-compiler:${versions.dagger}"
    databindingCompiler = "com.android.databinding:compiler:${versions.androidPlugin}"
    eventBus = "org.greenrobot:eventbus:${versions.eventBus}"
    constraintLayout = "com.android.support.constraint:constraint-layout:${versions.constraintLayout}"
    timber = "com.jakewharton.timber:timber:${versions.timber}"
    stetho = "com.facebook.stetho:stetho:${versions.stetho}"

    testingDependencies = [
            "junit:junit:${versions.junit}",
            "org.mockito:mockito-core:${versions.mockito}",
            "com.nhaarman:mockito-kotlin:${versions.mockitoKotlin}",
            "org.robolectric:robolectric:${versions.robolectric}"
    ]

    testingCompilerDependencies = [
            "com.google.dagger:dagger:${versions.dagger}",
            "com.google.dagger:dagger-compiler:${versions.dagger}"
    ]

    androidTestingDependencies = [
            "com.android.support:support-annotations:${versions.supportLib}",
            "com.android.support.test:runner:${versions.supportTest}",
            "com.android.support.test:rules:${versions.supportTest}"
    ]

    androidTestingCompilerDependencies = [
            "com.google.dagger:dagger:${versions.dagger}",
            "com.google.dagger:dagger-compiler:${versions.dagger}"
    ]

    androidTestingSupportDependencies = [
            "com.android.support.test.espresso:espresso-core:${versions.espresso}"
    ]

}